environments:
  preview:
  staging:
  production:
  dev:
  qa:
  qa1:
  qa2:
  qa3:
  qa4:
  hotfix:

helmfiles:
  - path: {{ requiredEnv "APPLICATION_HELMFILE" }}
    values:
      - /tmp/platform.yaml
      {{- if env "HELM_VALUES_FILE" }}
      - {{ env "HELM_VALUES_FILE" }}
      {{- end }}
